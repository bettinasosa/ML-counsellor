import OpenAI from 'openai';import {config} from "@/config";import Logger from "@/utils/logger";class OpenAIClient {    public client: OpenAI;    constructor() {        if (!config.openAI.apiKey) {            Logger.error('OpenAI API key is missing', new Error('Missing API key'));            throw new Error('OpenAI API key is required');        }        this.client = new OpenAI({            apiKey: config.openAI.apiKey,        });    }}export const openAIClient = new OpenAIClient();//TODO: Implement actual category extraction logic//// private extractCategories(text: string): string[] {//     const categories = ['job search', 'stress', 'motivation', 'technical skills', 'career advice', 'mental health'];//     return categories.filter(category => text.toLowerCase().includes(category));// }//// //TODO: Implement sentiment analysis// private analyzeSentiment(text: string): number {//     // some dummy sentiment analysis//     const positiveWords = ['happy', 'excited', 'confident', 'optimistic', 'encouraged'];//     const negativeWords = ['sad', 'anxious', 'stressed', 'disappointed', 'frustrated'];////     const words = text.toLowerCase().split(/\s+/);//     const positiveCount = words.filter(word => positiveWords.includes(word)).length;//     const negativeCount = words.filter(word => negativeWords.includes(word)).length;////     return (positiveCount - negativeCount) / words.length;// }