import React from 'react';import {useChatContext} from "@/context/chatContext";import {Card, CardContent, CardHeader, CardTitle} from "@/components/ui/card";export default function ContextWindowVisualizer() {    const {contextInfo} = useChatContext();    const sections = contextInfo.summary.split('\n\n');    return (        <Card className="w-full max-w-md mx-auto">            <CardHeader>                <CardTitle>Session Summary</CardTitle>            </CardHeader>            <CardContent>                {!contextInfo.summary && ( <p className='text-gray-500'>You will see some counselor notes here once you progress with you conversation.</p>)}                {sections.map((section, index) => {                    const [title, ...content] = section.split('\n');                    return (                        <div key={index} className="mb-4">                            <h3 className="font-bold mb-2">{title}</h3>                            <ul className="list-disc list-inside">                                {content.map((item, i) => (                                    <li key={i}>{item.replace('• ', '').replace(/^\d+\. /, '')}</li>                                ))}                            </ul>                        </div>                    );                })}            </CardContent>        </Card>    );}